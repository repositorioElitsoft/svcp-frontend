/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class',
    content: [
        "./src/**/*.{html,ts}", // Scans all HTML and TypeScript files in src
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};